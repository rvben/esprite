# esprite

Host-native ESP32/Arduino firmware simulator with an agent-facing CLI.

This package reserves the name. esprite is a C++/CMake tool; install it from
https://github.com/rvben/esprite (releases carry prebuilt binaries, or
`brew install rvben/tap/esprite`).
